// next-sitemap.config.js
/** @type {import('next-sitemap').IConfig} */
const config = {
  siteUrl: 'https://www.rdcvithna.com',
  generateRobotsTxt: true, // (optional) Generate a robots.txt file
  // Other options...
};

module.exports = config;
